# model-AB-testing Recipe

The **model-AB-testing** multimodal recipe was created manually in the Dataloop platform UI using the Evaluation Studio Form Builder.

The recipe uses a custom JS file (`model-AB-testing.js`) to avoid validation errors from the default JS template.
